# API Interface

The API interface exposes the engine to external systems.

## Responsibilities

- Accept structured requests  
- Validate inputs  
- Invoke the pipeline  
- Return deterministic outputs  
- Provide trace and error metadata  

## Requirements

- Stable contract  
- Edition‑aware behaviour  
- Clear error responses  
- Backward compatibility  
